import React from 'react';
import ReactDOM from 'react-dom/client';
import {BodyUploadForm} from './Body.jsx';

const root = ReactDOM.createRoot(document.getElementById('head'));
root.render(
  <BodyUploadForm />
);

